// Rewrite the exercise n. 24, by using the While loop.
// exercise n. 24 was already written by a while loop, the task didn't specify to write a "for" loop
// I use "while" loops because they are faster and require less code and they have more readability than the "for loop"
let i = 0;
while (++i <= 10) {
  console.log(i);
}
